프로젝트 목적 
MS Access 등에서 생성된 단일 대용량 급여명세서 PDF 파일(1페이지당 직원 1명)을 읽어, 각 페이지를 개별 PDF 파일로 분할하고 자동 분류하여 저장하는 고성능 Pure 프론트엔드(클라이언트 사이드) 웹 애플리케이션을 개발합니다.

아키텍처 및 파일 구성 요구사항
- 모든 코드는 외부 서버 전송 없이 사용자의 브라우저 내에서만 안전하게 동작하는 pure 프론트엔드 환경에서 동작해야 합니다. (개인정보 보호 최우선)
- 코드의 유지보수성과 시인성을 극대화하기 위해 다음 3개의 파일로 완벽히 분리하여 구성합니다:
    1. index.html - 웹 화면 구조 및 레이아웃 정의 (CSS, JS 라이브러리 CDN 연동)
    2. style.css - UI/UX 디자인 (옵시디언 다크 테마, 글래스모피즘, 반응형 그리드, 부드러운 애니메이션)
    3. app.js - PDF 분할, 텍스트 추출, 고성능 Regex 매칭, File System Access API 로컬 폴더 저장, UI 실시간 업데이트 및 ZIP 다운로드 로직
- 외부 PDF/ZIP 핵심 라이브러리는 CDN 방식으로 index.html 헤더에 통합합니다:
    - pdfjs-dist (v3.4.120): 고속 텍스트 추출 및 파싱
    - pdf-lib (v1.17.1): PDF 바이너리 분할 및 생성
    - jszip (v3.10.1): 클라이언트 사이드 일괄 ZIP 압축

핵심 기능 요구사항

1. UI/UX 및 입력 레이아웃 (index.html / style.css)
- 현대적이고 전문적인 느낌을 선사하는 옵시디언 다크 테마 및 Glassmorphism 기반 대시보드.
- 파일 업로드 영역 (Dropzone): PDF 파일을 마우스 드래그 앤 드롭하거나 탐색기에서 선택할 수 있는 직관적인 디자인.
- 기본 파일명 접두사 (Fallback) 입력: 텍스트 추출 실패 시 붙을 파일명 접두사를 설정할 수 있는 편의성 제공.
- 선택적 파일 저장 제어: 테이블 헤더의 '전체 선택' 체크박스 및 개별 행 앞의 선택 체크박스를 통해 저장할 대상을 자유롭게 선별할 수 있습니다.
- 실시간 프로그레스 바: "총 N명 중 i명째 분할/저장 중... (X%)" 문구와 상태 바를 60fps 기반 부드러운 애니메이션으로 표시.
- 실시간 파일 탐색기 (Explorer UI): 분할 완료된 파일 목록을 표 형식(선택 체크박스, 아이콘, 파일명, 유형, 용량, 개별 다운로드 버튼)으로 실시간 갱신 및 자동 스크롤.

2. 파일명 추출 및 저장 규칙 (app.js)
- 파일명 형식: [년월]_[직원이름]_급여명세서.pdf (예: 202605_홍길동_급여명세서.pdf)
- 2단계 고성능 정규식(Regex) 파서:
    1. 각 페이지의 텍스트 레이아웃을 파싱하여 급여년월 및 [성명]과 [소속부서] 사이의 성명/이름을 감지합니다.
    2. macOS NFD 한글 자모 분리 현상을 미연에 예방하기 위해 텍스트 추출 즉시 `text.normalize('NFC')` 및 `matchedName.normalize('NFC')`를 강제 적용합니다.
    3. 이름 추출 성공 후, 정규식 삭제 패턴 `replace(/(소속|부서|직급|직위|사원|사번|성명|이름|귀하).*$/, '')` 및 꼬리 키워드 필터를 통해 이름 뒤에 군더더기처럼 유입된 접미사를 완벽히 제거하여 무조건 순수한 이름만 남깁니다. (예: `202605_강호관소속_급여명세서.pdf` -> `202605_강호관_급여명세서.pdf`)
    4. 텍스트 추출에 완전히 실패한 스캔본이나 특이 서식의 경우, [Fallback입력값]_[순번]_급여명세서.pdf 형식으로 자동 Fallback 처리합니다.

3. 선택 파일 로컬 폴더 일괄 저장 및 날짜시간 폴더 자동화
- 변환과 저장의 완벽한 분리:
    - [분할 시작] 버튼 클릭 시에는 브라우저 메모리와 화면 리스트에만 실시간으로 안전하게 PDF 분할본을 생성합니다. (로컬 디렉토리 선택 필요 없음)
    - 분할이 완료된 후 사용자가 리스트에서 원하는 파일을 체크한 뒤, **[선택된 파일 로컬 저장]** 버튼을 누를 때 비로소 폴더 선택창이 열립니다.
- 저장 위치 및 동적 폴더명:
    - 저장 버튼을 누르고 **File System Access API (showDirectoryPicker)**를 통해 저장할 기준 폴더를 한 번 선택합니다.
    - 선택한 경로 하위에 분할 작업 시점의 **현재 날짜시간 폴더 (yyyymmdd_hhmmss 형식, 예: 20260522_222407)**를 자동으로 생성합니다.
    - 선택 체크박스에 표시된 파일들을 해당 날짜시간 폴더 내에 실시간으로 순차 기록합니다.
- 무경고 덮어쓰기 (Overwrite):
    - 동일한 날짜시간 폴더명이나 파일명이 이미 로컬에 존재하는 경우, 브라우저 경고창이나 확인창 없이 즉시 새 파일로 자동 덮어쓰기하여 업무 연속성을 보장합니다.
- 리스트 UX 보존 및 취소 처리 예외:
    - 사용자가 폴더 선택창을 띄운 뒤 '취소(Cancel)' 버튼을 누르더라도, 이전에 분할 완료되었던 화면 리스트와 체크박스 선택 상태는 **절대 지워지지 않고 그대로 보존**됩니다.

기술적 구현 및 성능 최적화 노하우 (app.js)

1. PDF.js Worker 버퍼 해제(Buffer Detachment) 에러 해결
- pdfjsLib.getDocument()는 기본적으로 ArrayBuffer의 소유권을 워커(Worker) 스레드로 넘겨 메인 스레드의 버퍼를 해제(Detach)시킵니다.
- 이로 인해 이후 pdf-lib 로딩 단계에서 ArrayBuffer is already detached 오류가 발생하는 문제를 예방하기 위해, 버퍼를 입력할 때 반드시 복사본을 전달합니다:
- // 복사본(.slice(0))을 전달하여 원본 버퍼 소유권을 메인 스레드에 보존
- pdfDoc = await pdfjsLib.getDocument({ data: originalArrayBuffer.slice(0) }).promise;
- const pdfLibDoc = await PDFLib.PDFDocument.load(originalArrayBuffer.slice(0));

2. 루프 내 개별 try-catch 격리 및 고성능 완주 파이프라인
- 특정 페이지의 파싱 에러나 순간적인 디스크 쓰기 오류(I/O Lock)로 전체 루프가 중단(Halt)되는 것을 완벽히 예방하기 위해, `for` 루프 내부의 각 페이지 처리 과정을 **개별 try-catch 블록으로 견고하게 격리**합니다.
- 로컬 저장 중 발생하는 쓰기 충돌을 해소하기 위해 **Exponential Backoff 대기가 가미된 3회 자동 재시도**를 장착하고, 매 루프 완료 시 **`80ms` 지연 대기(Throttling)**를 주어 브라우저와 OS가 I/O 자원을 정리할 수 있도록 확보합니다.
- 루프 내에서 무거운 PDF 파싱/저장 작업이 수행될 때 브라우저 탭이 얼어붙는 현상을 방지하고 프로그레스 바가 부드럽게 갱신되도록, requestAnimationFrame을 활용하여 UI 스레드에 실행 권한을 주기적으로 양보합니다:
- await new Promise(resolve => requestAnimationFrame(resolve));

